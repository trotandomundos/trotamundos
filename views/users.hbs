<h2>List of Users</h2>
{{#if errorMessage}}
  <div id="errorMessage">
    <p>{{errorMessage}}</p>
  </div>
{{/if}}
<div>
  {{#each users}}
    <div>
      <h2>{{this.username}}</h2>
      <h5>{{this.role}}</h5>
      <a href="/users/{{this._id}}">info</a>
      {{! //¿porque._id? }}
    </div>
  {{/each}}
</div>