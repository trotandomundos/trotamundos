<h2>Reviews list</h2>
<div class="marco">
  {{#each reviews}}
    <div class="marco">
      {{title}}
      {{rating}}
      {{comment}}
      {{location}}
      <a href="/myReviews/{{_id}}">Mis reviews</a>
    </div>
  {{/each}}
</div>
