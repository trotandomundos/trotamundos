<h1>Busca tu sitio</h1>
<div class="marco">
  <form action="/search-places" method="post">
    <label for="location">
      Ciudad
      <select name="location">
        <option value="Amsterdam">Amsterdam</option>
        <option value="Barcelona">Barcelona</option>
        <option value="Berlin">Berlin</option>
        <option value="Dubay">Dubay</option>
        <option value="London">London</option>
        <option value="Paris">Paris</option>
        <option value="Rome">Rome</option>
        <option value="Tuscany">Tuscany</option>
        <option value="">Ninguno</option>
      </select>
    </label>
    <label for="category">
      Categoria
      <select name="category">
        <option value="attraction">Atracción</option>
        <option value="restaurant">Restaurante</option>
        <option value="POI">Punto de interés</option>
        <option value="">Ninguna</option>
      </select>
    </label>
    <label for="name">
      Nombre del sitio
      <input type="text" name="name">
    </label>
    <button type="submit">Buscar sitio</button>
  </form>
</div>

<script src="/js/map.utility.js"></script>

{{!-- {{#if totalPages}}
  <div class="pagination">
    {{#if (ne currentPage 1)}}
      <a href="/search-places?page={{sub currentPage 1}}">Anterior</a>
    {{/if}}
    {{#each (range 1 totalPages)}}
      {{#if (eq this currentPage)}}
        <span>{{this}}</span>
      {{else}}
        <a href="/search-places?page={{this}}">{{this}}</a>
      {{/if}}
    {{/each}}
    {{#if (ne currentPage totalPages)}}
      <a href="/search-places?page={{add currentPage 1}}">Siguiente</a>
    {{/if}}
  </div>
{{/if}} --}}
