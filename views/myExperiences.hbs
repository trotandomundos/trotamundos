<h2>Experiences list</h2>

<div>
  {{#each experiences}}
    <div >
      {{titulo}}
      {{texto}}
      {{imagenes}}
      {{filtro}}
      <a href="/myExperiences/{{_id}}">Visitar</a>
    </div>
  {{/each}}
</div>